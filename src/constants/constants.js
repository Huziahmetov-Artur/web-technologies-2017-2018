const search = {
  title: ''
}

const sort = {
  field: 'title',
  up: 'up',
  down: 'down',
  language: 'en-ru'
}

const page = {
  offset: 0,
  limit: 5
}

const notFound = 'Sorry, no such film';

export const constants = {
  search,
  sort,
  page,
  notFound
}
